// this is my test program

void foo(decimal bar) {
	// do stuff here
	decimal foz = bar * bar;
	decimal baz = foz / 4;
};

decimal a = 8; 
decimal b = 2;

decimal c = a + b;
decimal d = a * b / (10 + a);

if (c < d) {
	decimal abc = 4;
	foo(abc);
};

decimal f;

f = a * b;
f = c + f;